<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <script src="https://cdn.tailwindcss.com"></script>
  <title>Edit Post</title>
  <style>
    body {
      font-family: 'Inter', sans-serif;
    }
  </style>
</head>
<body class="bg-gradient-to-b from-zinc-900 to-gray-900 text-white">

  <div class="w-full min-h-screen flex flex-col justify-center items-center p-10">
    
    <!-- Logout button -->
    <div class="w-full flex justify-end mb-6">
      <a class="bg-red-500 hover:bg-red-600 transition duration-200 rounded-lg px-4 py-2 text-sm" href="/logout">Logout</a>
    </div>
    
    <!-- Title -->
    <h3 class="text-4xl font-semibold text-center mb-10 text-gray-300">Edit Your Post</h3>

    <!-- Edit Form -->
    <form action="/update/<%= post._id %>" method="post" class="w-full max-w-2xl bg-zinc-800 p-8 rounded-xl shadow-xl space-y-6">
      
      <!-- Content textarea -->
      <textarea placeholder="What's on your mind?" class="w-full p-4 text-lg bg-transparent border-2 border-zinc-700 rounded-lg focus:outline-none focus:ring-2 focus:ring-yellow-500 transition-all" name="content"><%= post.content %></textarea>
      
      <!-- Update Button -->
      <input type="submit" class="px-6 py-3 w-full bg-yellow-500 text-black font-semibold rounded-lg hover:bg-yellow-600 transition duration-300" value="Update Post">
    </form>
  </div>

</body>
</html>
